<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.fallingintodreams.flying.bicycle.backend.dao.IUserAccountDao">
    <resultMap id="userAccountMap" type="top.fallingintodreams.flying.bicycle.backend.po.UserAccount">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="user_id" property="userId"/>
        <result column="state" property="state"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <insert id="insertUserAccount" parameterType="top.fallingintodreams.flying.bicycle.backend.po.UserAccount">
        INSERT INTO user_account (username, password, user_id, state, create_time, update_time)
        VALUES (#{username}, #{password}, #{userId}, #{state}, now(), now())
    </insert>

    <select id="getUserAccountByPassword" parameterType="top.fallingintodreams.flying.bicycle.backend.dto.UserAccountDTO"
            resultType="long">
        SELECT id
        FROM user_account
        WHERE username = #{username}
          and password = #{password}
          and state = 1
    </select>

    <select id="getUserAccount" parameterType="top.fallingintodreams.flying.bicycle.backend.dto.UserAccountDTO"
            resultType="long">
        SELECT id
        FROM user_account
        WHERE username = #{username}
          AND state = 1
    </select>

</mapper>